# 💕 Наша История - PWA приложение для пары

Романтическое веб-приложение для влюблённых пар с возможностью установки на iPhone как нативное приложение.

## 🎯 Функционал

- ✅ **Главный экран** - любовное сообщение с анимацией сердечек
- ✅ **Календарь дат** - важные даты с фото и описанием
- ✅ **Моменты** - лента воспоминаний с фотографиями
- ✅ **Статистика** - счётчик дней вместе, таймер до встречи, фразы дня
- ✅ **Планы** - список желаний и планов на будущее
- ✅ **PWA** - работает оффлайн, устанавливается на телефон

## 📋 Требования

- Node.js 18+ (скачать с https://nodejs.org/)
- Аккаунт Firebase (бесплатный)
- Аккаунт Vercel для деплоя (бесплатный)

## 🚀 Установка на Windows

### Шаг 1: Установите Node.js

1. Скачайте Node.js с https://nodejs.org/ (LTS версию)
2. Установите с настройками по умолчанию
3. Проверьте установку:
```bash
node -v
npm -v
```

### Шаг 2: Скачайте проект

Скачайте папку `couple-app` и распакуйте на рабочий стол.

### Шаг 3: Установите зависимости

Откройте PowerShell или командную строку:

```bash
cd Desktop\couple-app
npm install
```

### Шаг 4: Настройте Firebase

1. Откройте https://console.firebase.google.com/
2. Нажмите "Создать проект"
3. Введите название: "CoupleApp"
4. Отключите Google Analytics (не нужно)
5. Дождитесь создания проекта

#### Включите Authentication (Авторизация):

1. В левом меню выберите "Authentication"
2. Нажмите "Начать"
3. Выберите "Email/Password"
4. Включите и сохраните

#### Включите Firestore (База данных):

1. В левом меню выберите "Firestore Database"
2. Нажмите "Создать базу данных"
3. Выберите "Начать в тестовом режиме"
4. Выберите регион (europe-west)
5. Нажмите "Включить"

#### Включите Storage (Хранилище файлов):

1. В левом меню выберите "Storage"
2. Нажмите "Начать"
3. Выберите "Начать в тестовом режиме"
4. Нажмите "Готово"

#### Получите конфигурацию:

1. Нажмите на ⚙️ (Settings) → Project settings
2. Прокрутите вниз до "Your apps"
3. Нажмите на иконку Web (</>) 
4. Введите название: "couple-app"
5. НЕ ставьте галочку Firebase Hosting
6. Нажмите "Register app"
7. Скопируйте код из раздела `firebaseConfig`

### Шаг 5: Вставьте Firebase конфигурацию

Откройте файл `src/firebase/config.js` и замените:

```javascript
const firebaseConfig = {
  apiKey: "ВАШ_API_KEY",
  authDomain: "ВАШ_AUTH_DOMAIN",
  projectId: "ВАШ_PROJECT_ID",
  storageBucket: "ВАШ_STORAGE_BUCKET",
  messagingSenderId: "ВАШ_SENDER_ID",
  appId: "ВАШ_APP_ID"
};
```

### Шаг 6: Создайте иконки приложения

1. Откройте https://www.canva.com/
2. Создайте дизайн 512x512 пикселей
3. Добавьте сердечко или ваше фото
4. Скачайте как PNG
5. Используйте https://www.simicart.com/manifest-generator.html/ для генерации иконок
6. Сохраните файлы `icon-192.png` и `icon-512.png` в папку `public/icons/`

**Или используйте готовые:**
- Скачайте любое изображение сердечка
- Измените размер на 192x192 и 512x512 на https://www.iloveimg.com/resize-image

### Шаг 7: Запустите локально

```bash
npm run dev
```

Откройте в браузере: http://localhost:3000

## 🌐 Деплой на Vercel

### Шаг 1: Создайте аккаунт

1. Откройте https://vercel.com/
2. Нажмите "Sign Up"
3. Войдите через GitHub (создайте аккаунт GitHub если нет)

### Шаг 2: Установите Vercel CLI

```bash
npm install -g vercel
```

### Шаг 3: Задеплойте проект

В папке проекта выполните:

```bash
vercel login
vercel
```

Ответьте на вопросы:
- Set up and deploy? → **Y**
- Which scope? → Выберите ваш аккаунт
- Link to existing project? → **N**
- What's your project's name? → **couple-app**
- In which directory is your code located? → `.` (точка)
- Want to override the settings? → **N**

Подождите 1-2 минуты. Vercel выдаст вам URL типа:
```
https://couple-app-xxxxx.vercel.app
```

## 📱 Установка на iPhone

### Вариант 1: Через Safari (рекомендуется)

1. Откройте ваш URL в Safari на iPhone
2. Нажмите кнопку "Поделиться" (квадрат со стрелкой вверх)
3. Прокрутите вниз и выберите "На экран Домой"
4. Нажмите "Добавить"
5. Приложение появится на главном экране!

### Вариант 2: QR-код

1. Создайте QR-код вашего URL на https://www.qr-code-generator.com/
2. Отсканируйте камерой iPhone
3. Откройте в Safari
4. Добавьте на главный экран (см. Вариант 1)

## 🎨 Кастомизация

### Изменить цвета

Откройте `src/app/globals.css` и измените:

```css
.bg-pink-500 { background-color: #ВАШ_ЦВЕТ; }
```

### Изменить анимацию сердечек

Откройте `src/components/Hearts.jsx` и измените:
- `duration` - скорость анимации
- `size` - размер сердечек
- частоту появления (300ms в setInterval)

### Добавить свои фразы

Откройте `src/components/Stats.jsx` и добавьте в массив `phrases`:

```javascript
const phrases = [
  "Ваша фраза 1",
  "Ваша фраза 2",
  // ...
]
```

## 🔒 Безопасность

### Правила Firebase Security

После тестирования настройте правила безопасности:

1. Откройте Firebase Console
2. Firestore Database → Rules
3. Вставьте:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /couples/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

4. Storage → Rules:

```
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

## 🐛 Решение проблем

### Ошибка "Firebase not configured"

Проверьте, что вы правильно скопировали конфигурацию в `src/firebase/config.js`

### Фото не загружаются

Убедитесь, что включили Firebase Storage и настроили правила безопасности.

### Приложение не устанавливается на iPhone

- Используйте только Safari (не Chrome/Firefox)
- Проверьте, что иконки находятся в `public/icons/`
- Очистите кэш браузера

### "Module not found"

```bash
rm -rf node_modules package-lock.json
npm install
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12 → Console)
2. Проверьте Firebase Console на наличие ошибок
3. Убедитесь, что все зависимости установлены: `npm install`

## 🎁 Советы для подарка

1. **Перед дарением:**
   - Зарегистрируйте аккаунт
   - Добавьте несколько дат и моментов
   - Установите дату начала отношений
   - Напишите милое сообщение на главном экране

2. **Как подарить:**
   - Установите приложение на её телефон
   - Покажите основные функции
   - Дайте логин и пароль на красивой открытке

3. **Идеи для контента:**
   - Первое свидание
   - Первый поцелуй
   - Совместные путешествия
   - Смешные моменты
   - Планы на будущее

## 📄 Лицензия

Создано с ❤️ для вашей пары. Используйте свободно!
#   p a c k a g e . j s o n  
 